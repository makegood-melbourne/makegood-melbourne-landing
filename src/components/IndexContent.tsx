// This component is no longer used - all sections are now directly imported in index.astro
// Keeping this file for backwards compatibility but it's essentially deprecated

const IndexContent = () => {
  return null;
};

export default IndexContent;
